[package]
name = "libsqlite3-sys"
version = "0.36.0"
edition = "2021"
links = "sqlite3"
license = "MIT"
description = "SQLite3 FFI bindings"
build = "build.rs"

[lib]
name = "libsqlite3_sys"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"

[features]
default = []
bundled = []